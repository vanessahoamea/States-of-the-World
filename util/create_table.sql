CREATE TABLE countries (
    name VARCHAR(100),
    capital VARCHAR(100),
    languages JSON,
    population INTEGER,
    density DOUBLE,
    area DOUBLE,
    time_zone VARCHAR(10),
    currency VARCHAR(5),
    government VARCHAR(150)
);